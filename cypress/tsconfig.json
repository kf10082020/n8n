{
	// 📦 Наследуем базовую конфигурацию проекта
	"extends": "../tsconfig.json",

	// ⚙️ Специфические настройки компилятора для e2e-тестов
	"compilerOptions": {
		// 🔕 Отключаем sourcemaps и декларации для Cypress-тестов
		"sourceMap": false,
		"declaration": false,

		// 🧠 Используем современные ECMAScript возможности и DOM API
		"lib": ["esnext", "dom"],

		// 🧪 Типы, необходимые для Cypress и сопутствующих библиотек
		"types": ["cypress", "node", "cypress-real-events"],

		// 📁 Улучшения совместимости при линке с другими модулями (например, `packages/workflow`)
		"moduleResolution": "node",
		"esModuleInterop": true,
		"resolveJsonModule": true,
		"isolatedModules": false,
		"skipLibCheck": true,
		"noEmit": true
	},

	// 📂 Файлы, которые включаются в компиляцию
	"include": ["**/*.ts"],

	// ❌ Исключения (не компилируем сгенерированные и сторонние файлы)
	"exclude": ["**/dist/**/*", "**/node_modules/**/*"],

	// 🔗 Связь с зависимым проектом (используется в проектных ссылках monorepo)
	"references": [
		{ "path": "../packages/workflow/tsconfig.build.json" }
	]
}
